package com.example.nativemovieapp.adapter;

import android.view.View;
import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;
import org.jetbrains.annotations.NotNull;

public class DetailTrailerAdapter {

    public class DetailTrailerViewHolder extends RecyclerView.ViewHolder {
        public DetailTrailerViewHolder(@NonNull @NotNull View itemView) {
            super(itemView);
        }
    }
}
